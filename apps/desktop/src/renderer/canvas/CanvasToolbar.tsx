import { Redo2, SquareDashedMousePointer, Undo2 } from "lucide-react";
export function CanvasToolbar({ readOnly, onPrompt, onImage, onGroup, onModule, onUndo, onRedo }: { readOnly: boolean; onPrompt(): void; onImage(): void; onGroup(): void; onModule(): void; onUndo(): void; onRedo(): void }) { return <nav className="canvas-toolbar" aria-label="Canvas commands"><button type="button" aria-label="Add prompt from canvas" title="Add prompt from canvas" onClick={onPrompt} disabled={readOnly}>Quick prompt</button><button type="button" aria-label="Add image from canvas" title="Add image from canvas" onClick={onImage} disabled={readOnly}>Quick image</button><span /><button type="button" onClick={onGroup} disabled={readOnly}><SquareDashedMousePointer size={14} /> Group</button><button type="button" onClick={onModule} disabled={readOnly}>Module</button><span /><button type="button" aria-label="Undo graph transaction" onClick={onUndo} disabled={readOnly}><Undo2 size={15} /></button><button type="button" aria-label="Redo graph transaction" onClick={onRedo} disabled={readOnly}><Redo2 size={15} /></button></nav>; }
