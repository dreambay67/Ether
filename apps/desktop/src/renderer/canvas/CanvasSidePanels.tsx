import type { EtherGraph } from "@ether/schema";
export function CanvasSidePanels({ graph, selectedIds, status, runPrompt, runLabel, runDetail, runBusy, onRunSelected, onDismissRun, onLeave, onExposeParameter }: { graph: EtherGraph; selectedIds: string[]; status: string; runPrompt: boolean; runLabel: string; runDetail: string; runBusy: boolean; onRunSelected(): void; onDismissRun(): void; onLeave?(): void; onExposeParameter?(nodeId: string): void }) { return <><aside className="canvas-legend" aria-label="Canvas legend"><strong>{graph.title}</strong><span>{graph.nodes.length} nodes - {graph.edges.length} lanes</span><small>Left drag selects - right drag pans</small>{onLeave ? <button type="button" onClick={onLeave}>Leave module</button> : null}{onExposeParameter && selectedIds.length === 1 ? <button type="button" onClick={() => onExposeParameter(selectedIds[0]!)}>Expose selected parameter</button> : null}</aside>{runPrompt ? <aside className="canvas-selection-run-prompt" aria-label="Selected run prompt"><strong>{selectedIds.length} nodes selected</strong><p>{runDetail}</p><div><button type="button" disabled={runBusy} onClick={onRunSelected}>{runLabel}</button><button type="button" disabled={runBusy} onClick={onDismissRun}>Dismiss</button></div></aside> : null}<div className="canvas-status" data-testid="canvas-status" aria-live="polite">{status}</div></>; }
