# T03 automated evidence map: document lifecycle and crash/recovery

This is the supporting **A (automated)** audit for T03. It maps all 37
`AC-A02-*` and `AC-A03-*` requirements to tests that exist in this checkout.
`A-PASS` means the named automated assertion covers the deterministic part of
the requirement; `A-PARTIAL` means the test is useful evidence but leaves a
scope or UI/OS assertion open; `A-GAP` means no qualifying automated assertion
was found. A result here never supplies required packaged (`P`) or owner (`M`)
evidence, and does not close T03 or Phase 0.

The focused runs were executed with `pnpm.cmd` on the fixer checkout. The
historical baseline below is retained for audit traceability and is not the
current fixer result:

| Command | Result |
| --- | --- |
| `pnpm.cmd --dir packages/testing exec vitest run --config vitest.integration.config.ts tests/desktop/document-lifecycle.spec.ts tests/document-chaos.integration.ts tests/document-recovery.test.ts tests/document-compatibility-4x.test.ts tests/document-format.test.ts tests/document-blobs.test.ts tests/document-locking.test.ts` | 6 files, 193 passed, 0 failed |
| `pnpm.cmd --dir packages/testing exec vitest run --config vitest.integration.config.ts tests/document-graph-repository.test.ts tests/application-durability.test.ts tests/durable-repositories.test.ts tests/application-vertical-slice.test.ts` | 4 files, 75 passed, 0 failed |
| `pnpm.cmd --dir packages/testing exec vitest run --config vitest.config.ts tests/desktop-ipc-contract.test.ts tests/recovery-journey-driver.test.ts tests/acceptance-docs.test.ts` | 3 files, 26 passed, 0 failed |
| `$env:ETHER_RUN_DESTRUCTIVE_CHAOS='1'; $env:ETHER_TEST_SUITE='destructive-chaos'; pnpm.cmd --dir packages/testing exec vitest run --config vitest.integration.config.ts tests/document-chaos.integration.ts` | 1 file, 15 passed, 0 failed |

Historical baseline total: **14 files and 309 passed tests, 0 failures**. The Playwright
`document-lifecycle-recovery.spec.ts` is intentionally not used as A evidence:
it is the required packaged journey (P) and is skipped unless the Windows
journey mode is explicitly run. No row below claims that a packaged journey or
manual owner review has happened.

Current fixer verification was rerun at source HEAD
`7c11d8b742cb0aaa21b24649fa497c322597e7ca` on 2026-08-03. C1, C2, C3, and C4
are the same four command groups above; C5 is the separate Windows contract
check reported by the phase auditor:

| Current check | Result |
| --- | --- |
| C1 lifecycle/recovery group | 6 files, 205 passed, 0 failed |
| C2 repository/durability group — `pnpm.cmd --dir packages/testing exec vitest run --config vitest.integration.config.ts tests/document-graph-repository.test.ts tests/application-durability.test.ts tests/durable-repositories.test.ts tests/application-vertical-slice.test.ts` | 4 files, 75 passed, 0 failed |
| C3 contract-driver group | 3 files, 31 passed, 0 failed |
| C4 destructive-chaos group | 1 file, 15 passed, 0 failed |
| C5 Windows integration contract | 1 file, 25 passed, 0 failed |

The current C1–C4 rerun is **14 files and 326 passed tests, 0 failures**;
C5 is reported separately and is not folded into that total. The earlier
`document-graph-repository.test.ts` `revisionKeys` failure caused by the newly
exposed `revisions.listHistory` facade is retained as a historical diagnosis;
commit `7c11d8b` corrected the contract assertion, so it is not a current
failure.

Additional focused runs for this update:

| Command | Result |
| --- | --- |
| `pnpm.cmd --dir packages/testing exec vitest run --config vitest.integration.config.ts tests/desktop/document-lifecycle.spec.ts tests/application-vertical-slice.test.ts` | 2 files, 89 passed, 0 failed |
| `pnpm.cmd --dir packages/testing exec playwright test --config playwright.config.ts tests/desktop/document-history.spec.ts` | 2 source-browser tests passed, 0 failed |
| `pnpm.cmd --dir packages/testing exec playwright test --config playwright.desktop.config.ts tests/desktop/document-lifecycle.electron.spec.ts` | 12 source-Electron tests passed, 0 failed |

## Document lifecycle (AC-A02)

| ID | Acceptance requirement | Exact automated evidence (class A) | Audit result and uncovered scope |
| --- | --- | --- | --- |
| AC-A02-001 | File > New opens an untitled usable canvas without asking for a parent directory. | `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `creates New without invoking a parent picker or save dialog`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `creates an immediate untitled AppData-owned document without exposing its path`; `packages/testing/tests/desktop/document-lifecycle.electron.spec.ts` — `real Electron persists canvas edits and serves embedded artifacts through the secure protocol` (clicks New document). | **A-PARTIAL** — the service and source-Electron route prove New is AppData-owned and does not call either dialog port; the native Windows picker surface remains packaged P evidence. |
| AC-A02-002 | Ctrl+S opens the Windows Save dialog for an untitled document. | `packages/testing/tests/desktop/document-history.spec.ts` — `Ctrl+S routes to the document bridge, records a visible manual milestone, and recovery remains reviewable`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `saves, saves as, saves a copy, compacts, and reopens one validated Ether file`. | **A-PARTIAL** — source-browser keyboard routing and dialog-port publication are covered; the native Windows Save dialog surface remains packaged P evidence. |
| AC-A02-003 | Saving produces one `.ether` file and no required adjacent project directory. | `packages/testing/tests/document-format.test.ts` — `creates one validated Campaign.ether file with the frozen 4.0 identity`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `saves, saves as, saves a copy, compacts, and reopens one validated Ether file`; `packages/testing/tests/application-vertical-slice.test.ts` — `persists and executes the exact Campaign.ether plan, then reopens its artifact read-only`. | **A-PASS** for one-file publication/no folder-era sidecar in the tested roots. |
| AC-A02-004 | Writer leases and recovery metadata are stored in Ether AppData, not beside the document. | `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `keeps leases and recovery metadata under AppData, never beside the Ether file`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `creates an immediate untitled AppData-owned document without exposing its path`; `packages/testing/tests/document-locking.test.ts` — `reconciles a killed Save As replacement from its AppData recovery journal`. | **A-PASS** for the deterministic source-service contract: active lease metadata is present under AppData and the document directory contains only the `.ether` file; packaged/OS metadata placement remains P. |
| AC-A02-005 | A clean close leaves no rollback journal; crash recovery removes or resolves a stale journal before writable open. | `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `closes cleanly, then reconciles a stale journal before a writable reopen`; `packages/testing/tests/document-locking.test.ts` — `recovers Compact interrupted at vacuum without orphan recovery files`; `packages/testing/tests/document-locking.test.ts` — `reconciles a killed Save As replacement from its AppData recovery journal`. | **A-PASS** for the deterministic lifecycle: clean close leaves only the Ether file, a staged stale journal is reconciled before writable admission, and journal/staging/rollback paths are removed. |
| AC-A02-006 | The saved file carries Ether application ID, marker, format, and schema identifiers. | `packages/testing/tests/document-format.test.ts` — `creates one validated Campaign.ether file with the frozen 4.0 identity`; `packages/testing/tests/document-format.test.ts` — `exports immutable Ether 4.0 format metadata after the Phase 5 freeze`. | **A-PASS** — format marker, application ID, format/schema versions, and SQLite pragmas are asserted. |
| AC-A02-007 | Ordinary graph edits autosave and visibly transition through Saving to Saved. | `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `starts after 1.5 seconds idle, caps first-dirty age at 10 seconds, and serializes saves`; `packages/testing/tests/desktop/document-lifecycle.electron.spec.ts` — `real Electron persists canvas edits and serves embedded artifacts through the secure protocol` (observes Saving/Saved); `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `preserves dirty state and reports attention after a failed save`. | **A-PASS** for source-Electron automated UI plus coordinator state; packaged scale and owner visual evidence remain open. |
| AC-A02-008 | Ctrl+S creates a manual milestone visible in document history. | `packages/testing/tests/desktop/document-history.spec.ts` — `Ctrl+S routes to the document bridge, records a visible manual milestone, and recovery remains reviewable`; `packages/testing/tests/document-graph-repository.test.ts` — `stores autosave and manual milestones at document and all graph heads without empty graph revisions`; `packages/testing/tests/application-durability.test.ts` — `deduplicates save milestones by durable command ID`. | **A-PASS** for source-browser UI plus durable persistence; the packaged history surface remains P evidence. |
| AC-A02-009 | Closing after a successful autosave does not show a false unsaved-changes prompt. | `packages/testing/tests/desktop/document-lifecycle.electron.spec.ts` — `real Electron quits after a successful autosave without a false unsaved-changes prompt`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `waits for an in-flight autosave before closing the active document`; `packages/testing/tests/desktop/document-lifecycle.electron.spec.ts` — `real Electron delays quit until an in-flight autosave lifecycle operation drains`. | **A-PARTIAL** — source Electron quit completes after Saved without a prompt and lifecycle drain is deterministic; native packaged prompt rendering remains P evidence. |
| AC-A02-010 | File > Open opens a valid `.ether` document. | `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `saves, saves as, saves a copy, compacts, and reopens one validated Ether file`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `keeps a fake-provider PNG embedded after save, close, and reopen`; `packages/testing/tests/desktop/document-lifecycle.electron.spec.ts` — `real Electron persists canvas edits and serves embedded artifacts through the secure protocol` (opens through the native menu). | **A-PASS** for validated service/native-menu open; the real packaged File > Open dialog remains P evidence. |
| AC-A02-011 | Explorer double-click opens the correct document in Ether. | `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `routes picker, drop, argv, second-instance, and open-file through one coordinator` (argv/open-file routing). | **A-GAP** — no automated test launches Explorer or performs a Windows shell double-click. |
| AC-A02-012 | Dragging an `.ether` document onto Ether opens it. | `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `routes picker, drop, argv, second-instance, and open-file through one coordinator`; `packages/testing/tests/desktop/document-lifecycle.electron.spec.ts` — `real Electron persists canvas edits and serves embedded artifacts through the secure protocol` (file-drop helper). | **A-PARTIAL** — coordinator/drop bridge coverage exists, but no real OS drag gesture onto the installed window is asserted. |
| AC-A02-013 | Opening an already-open document focuses its window. | `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `canonicalizes open requests and focuses an already-open identity`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `routes picker, drop, argv, second-instance, and open-file through one coordinator`. | **A-PASS** for canonical identity/focus routing in the desktop coordinator; multi-process shell focus is still P. |
| AC-A02-014 | A competing process cannot acquire writable access; read-only open remains available. | `packages/testing/tests/document-locking.test.ts` — `allows one writer, downgrades prefer-write competitors, and rejects require-write with a typed error`; `packages/testing/tests/document-chaos.integration.ts` — `refuses concurrent, cloud, UNC-classified, and mapped-drive writers while keeping read-only access`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `preserves a competing writer reason and exposes only read-only-safe document commands`. | **A-PASS** — writer lease, location rejection, read-only mode, and command gating are asserted. |
| AC-A02-015 | A cold native fixed-volume probe can complete after nine seconds, while location classification remains fail-closed and hard-bounded at 12 seconds. | `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `allows a cold native probe beyond eight seconds while retaining a hard default bound`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `enforces the normative 12-second default native probe bound`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `times out and cancels a hanging native probe without blocking the event loop`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `cancels an actual hanging probe child without blocking the event loop`. | **A-PASS** for the source capability contract: a 9-second probe completes and the omitted option uses the normative 12,000ms timeout with cancellation/fail-closed classification. |
| AC-A02-016 | Recent Documents and Jump List entries open their target or report that it is missing. | `packages/testing/tests/desktop-settings-store.test.ts` — `persists canonical document identities and deduplicates paths`; `packages/testing/tests/desktop-settings-store.test.ts` — `serializes concurrent recent-document updates without losing identities`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `routes picker, drop, argv, second-instance, and open-file through one coordinator`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `reports a missing recent or Jump List target through the main open route`. | **A-PARTIAL** — recent identity and main-route missing-target handling are covered; actual Jump List shell activation/reporting remains packaged P evidence. |
| AC-A02-017 | Unicode, spaces, long paths, removable drives, and local cloud-synced locations behave predictably. | `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `saves, saves as, saves a copy, compacts, and reopens one validated Ether file` (spaces/Unicode); `packages/testing/tests/document-format.test.ts` — `uses no-create private-cache file URLs for missing and Unicode writable paths`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — parameterized `classifies %s from its final native path and volume` (the `removable drive` case); `packages/testing/tests/desktop/document-lifecycle.spec.ts` — parameterized `classifies %s from its final native path and volume` (the `extended-length fixed path` case); `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `keeps a drive-root cloud directory rooted when Ether runs from that drive`. | **A-PARTIAL** — path classification and Unicode/extended-path logic are covered, but no one test performs a complete save/reopen on removable and cloud-synced locations. |
| AC-A02-018 | Save As produces a complete validated destination and switches only after success. | `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `saves, saves as, saves a copy, compacts, and reopens one validated Ether file`; `packages/testing/tests/document-locking.test.ts` — `uses validated no-clobber backups for Save a Copy and Save As with independent identities and leases`; `packages/testing/tests/document-locking.test.ts` — `Save As from read-only acquires the destination lease and switches only after validation`. | **A-PASS** — destination validation/publication and post-success identity switch are asserted. |
| AC-A02-019 | Save As rebinds its AppData lease to the destination and leaves no persistent source-path lock. | `packages/testing/tests/document-locking.test.ts` — `uses validated no-clobber backups for Save a Copy and Save As with independent identities and leases`; `packages/testing/tests/document-locking.test.ts` — `never leaves two writable stores when releasing the source lease times out during Save As`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `reissues real linked-reference grants across reopen, Save As, and Save Copy`. | **A-PASS** — independent destination/source leases and handoff cleanup are asserted, including timeout safety. |
| AC-A02-020 | Save a Copy produces a complete validated destination without switching. | `packages/testing/tests/document-locking.test.ts` — `uses validated no-clobber backups for Save a Copy and Save As with independent identities and leases`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `saves, saves as, saves a copy, compacts, and reopens one validated Ether file`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `reissues real linked-reference grants across reopen, Save As, and Save Copy`. | **A-PASS** — destination file and unchanged active identity are asserted. |
| AC-A02-021 | Failed Save As leaves the source and pre-existing destination intact. | `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `does not switch identity or damage an existing destination when Save As validation fails`; `packages/testing/tests/document-locking.test.ts` — `preserves an existing Save As destination through every injected pre-publication failure`; `packages/testing/tests/document-chaos.integration.ts` — `preserves source and pre-existing destination after a real hard kill during Save As publication`. | **A-PASS** — injected and hard-kill publication failures preserve both paths and active identity. |
| AC-A02-022 | Compact Document preserves graph, artifacts, lineage, runs, and hashes. | `packages/testing/tests/application-vertical-slice.test.ts` — `persists and executes the exact Campaign.ether plan, then reopens its artifact read-only` (snapshots graph, artifacts, lineage, run, plan/content hashes before and after `compactDocument`); `packages/testing/tests/document-locking.test.ts` — `restores the active document when staged Compact fails after publication`. | **A-PASS** for field-by-field source application preservation; packaged compact visual confirmation remains P evidence. |
| AC-A02-023 | Compact reports actual before/after size and never replaces the source with an invalid file. | `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `saves, saves as, saves a copy, compacts, and reopens one validated Ether file`; `packages/testing/tests/desktop/document-lifecycle.electron.spec.ts` — `real Electron persists canvas edits and serves embedded artifacts through the secure protocol` (compact status); `packages/testing/tests/document-locking.test.ts` — `restores the active document when staged Compact fails after publication`; `packages/testing/tests/document-chaos.integration.ts` — `preserves the original after a real hard kill during Compact replacement preparation`. | **A-PASS** for size reporting and no-invalid-replacement behavior in the tested paths; packaged visual confirmation remains P. |
| AC-A02-024 | A document with embedded references opens fully after being moved to another supported Windows machine. | `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `keeps a fake-provider PNG embedded after save, close, and reopen`; `packages/testing/tests/application-vertical-slice.test.ts` — `persists and executes the exact Campaign.ether plan, then reopens its artifact read-only`; `packages/testing/tests/desktop/document-lifecycle.electron.spec.ts` — `real Electron persists canvas edits and serves embedded artifacts through the secure protocol`. | **A-PARTIAL** — embedded round-trip and secure asset serving are local; no automated cross-machine/moved-volume profile is exercised. |
| AC-A02-025 | Make Document Portable reports missing references and embeds every available linked source. | `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `preflights portable references, confirms natively, and makes no change on cancellation`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `reports successful portable byte counts and identifiable missing references`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `calculates portable embed count and byte size without mutating references`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `reclaims a Portable blob finalized immediately before import failure and can retry after reopen`. | **A-PASS** — preflight, missing-link reporting, byte/count accounting, no-op cancellation, and retry cleanup are asserted. |
| AC-A02-026 | Deleting Ether caches, provider staging, and Live Output Folder does not remove project data. | `packages/testing/tests/application-vertical-slice.test.ts` — `persists and executes the exact Campaign.ether plan, then reopens its artifact read-only` (deletes explicit test-owned cache, staging, and Live Output roots before graph/artifact reopen checks); `packages/testing/tests/document-chaos.integration.ts` — `reconciles a real hard kill during an optional Live Output move without touching the embedded original`; `packages/testing/tests/document-recovery.test.ts` — `leaves another document's journal and staging completely untouched`. | **A-PASS** for explicit disposable-root deletion followed by graph/artifact/run/lineage integrity; actual installed-user cleanup remains P evidence. |

## Crash and recovery (AC-A03)

| ID | Acceptance requirement | Exact automated evidence (class A) | Audit result and uncovered scope |
| --- | --- | --- | --- |
| AC-A03-001 | Kill during graph autosave restores the last complete transaction. | `packages/testing/tests/document-chaos.integration.ts` — `rolls back an in-flight document transaction after a real hard kill`; `packages/testing/tests/application-durability.test.ts` — `recovers process-lost running work after close and reopen`. | **A-PASS** — hard-kill transaction rollback reopens the prior complete title/graph and clears dirty state. |
| AC-A03-002 | Kill during a large embedded import exposes no partial artifact. | `packages/testing/tests/document-chaos.integration.ts` — `reclaims a real hard-killed blob import without exposing a partial blob or artifact`; `packages/testing/tests/document-blobs.test.ts` — `keeps a partially published chunk import invisible and safely reclaimable`. | **A-PASS** — 4 MiB hard-kill and partial-chunk invisibility/reclaim assertions pass. |
| AC-A03-003 | Interrupted imports resume or are safely reclaimed. | `packages/testing/tests/document-chaos.integration.ts` — `reclaims interrupted blob import staging and rejects executable and MIME-confused content`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `reclaims a Portable blob finalized immediately before import failure and can retry after reopen`; `packages/testing/tests/document-blobs.test.ts` — `keeps a partially published chunk import invisible and safely reclaimable`. | **A-PASS** — safe reclaim and a retry-after-reopen path are both automated. |
| AC-A03-004 | Kill after provider output but before document import reconciles or quarantines staged output. | `packages/testing/tests/document-chaos.integration.ts` — `reconciles a real hard kill after provider completion is staged but before document import`; `packages/testing/tests/application-durability.test.ts` — `recovers after the provider process is hard-killed at provider-output-staged`; `packages/testing/tests/document-recovery.test.ts` — `reconciles a staged provider output once and is idempotent`. | **A-PASS** — staged output is imported once or quarantined with idempotent recovery. |
| AC-A03-005 | Kill during Save As preserves the original and any pre-existing destination. | `packages/testing/tests/document-chaos.integration.ts` — `preserves source and pre-existing destination after a real hard kill during Save As publication`; `packages/testing/tests/document-locking.test.ts` — `reconciles a killed Save As replacement from its AppData recovery journal`; `packages/testing/tests/document-locking.test.ts` — `preserves an existing Save As destination through every injected pre-publication failure`. | **A-PASS** — hard-kill/journal reconciliation and injected publication failures preserve both files. |
| AC-A03-006 | Kill during Compact preserves the original. | `packages/testing/tests/document-chaos.integration.ts` — `preserves the original after a real hard kill during Compact replacement preparation`; `packages/testing/tests/document-locking.test.ts` — `recovers Compact interrupted at vacuum without orphan recovery files`; `packages/testing/tests/document-locking.test.ts` — `restores the active document when staged Compact fails after publication`. | **A-PASS** — original bytes/identity survive kill and injected compact stages. |
| AC-A03-007 | Recovery work is shown as a reviewable revision and is not applied invisibly. | `packages/testing/tests/document-chaos.integration.ts` — `imports a durable provider completion once as review-required recovery`; `packages/testing/tests/document-recovery.test.ts` — `reconciles a staged provider output once and is idempotent`; `packages/testing/tests/application-durability.test.ts` — `accepts a durably staged provider result on reopen without a second provider call`. | **A-PASS** for durable `reviewRequired` metadata, dirty/review state, and one-time reconciliation; visible reviewer UI is P evidence. |
| AC-A03-008 | Deliberately corrupt metadata opens read-only recovery or a clear unsupported state. | `packages/testing/tests/document-format.test.ts` — `refuses malformed, future, and unsupported Ether metadata without changing the file`; `packages/testing/tests/desktop/document-lifecycle.spec.ts` — `keeps the active document, graph, lease, and commands when corrupt Open validation fails`; `packages/testing/tests/document-compatibility-4x.test.ts` — `refuses a future major before it can be opened for normal use`. | **A-PASS** — typed unsupported/corrupt outcomes are fail-closed and source bytes remain unchanged. |
| AC-A03-009 | Deliberately corrupt indexes are rebuildable. | `packages/testing/tests/document-compatibility-4x.test.ts` — `repairs supported derived-index corruption through read-only recovery without mutating the source`; `packages/testing/tests/document-recovery.test.ts` — `repairs through corrupt derived FTS state and rebuilds it from authoritative rows`. | **A-PASS** — derived FTS corruption is rebuilt into a new valid file while the source remains unchanged. |
| AC-A03-010 | Deliberately corrupt media chunks report affected artifacts distinctly from graph corruption. | `packages/testing/tests/desktop/document-history.spec.ts` — `repair review groups media and graph losses into distinct UI sections`; `packages/testing/tests/document-blobs.test.ts` — `detects corrupt chunk length or hash before returning bytes`; `packages/testing/tests/document-recovery.test.ts` — `repairs into a fresh schema-40000 file, rehashes blobs, and reports corrupt losses`. | **A-PASS** for source renderer classification plus deterministic media-loss reporting; packaged damaged-media UI remains P evidence. |
| AC-A03-011 | Repair writes a new `.ether` file and report; it never mutates the damaged source. | `packages/testing/tests/document-chaos.integration.ts` — `repairs into a reviewable new document and report without mutating the damaged source`; `packages/testing/tests/document-compatibility-4x.test.ts` — `repairs supported derived-index corruption through read-only recovery without mutating the source`; `packages/testing/tests/document-recovery.test.ts` — `repairs into a fresh schema-40000 file, rehashes blobs, and reports corrupt losses`. | **A-PASS** — destination/report, source hash preservation, and fresh schema validation are asserted. |

## Packaged evidence reconciliation (separate from this A map)

At HEAD `5417ce1999f04206918ef9aaf8fae1eac2ef33c8`, the installed package retained
four passing lifecycle records and six passing visible-recovery records. Each result
records the same `Ether.exe` SHA-256
`03db599dbaec1e6d564275cfb63d568dec0189217237efa3ea5e2e99b482580f`, `app.asar`
SHA-256 `8cc07d197b636b5104da29ffc295313ed64f82082cf2907566f5cbfedafb0a4e`, fresh
isolated profile, 1280x720/scale-1 viewport, and `errors: []`. Read-only postchecks
found no exact packaged Fixer `Ether.exe` process. The lifecycle records cover native
save, one-file publication, Save As/Copy/Compact/Portable controls, manual History,
autosave, hard-kill reopen, clean UIA close/reopen, and competing-writer read-only
state. The visible records cover review-required provider recovery, unsupported
metadata, and distinct media/artifact versus graph repair losses.

This appended reconciliation does not rewrite the A classifications above, does not
promote the T01 ledger, and does not close the association, Explorer drag, Jump List,
cross-machine, long-path/removable/cloud, or owner-M requirements. See
`packaged-coverage-plan.md` for requirement-level boundaries and retained evidence
paths.

## Normal A02 packaged evidence cross-reference

The canonical normal A02 route was reconciled at evidence identity commit
`14dda6c0aa33398bfdb4424ad21580d67ffd44b1` using `Ether.exe` SHA-256
`03db599dbaec1e6d564275cfb63d568dec0189217237efa3ea5e2e99b482580f` and `app.asar`
SHA-256 `8cc07d197b636b5104da29ffc295313ed64f82082cf2907566f5cbfedafb0a4e`.
The primary and reopen records under
`phase-0/document-windows-integration/a02-windows-primary/packaged/` and
`a02-windows-reopen-after-cleanup/packaged/` each passed with a fresh isolated
profile, 1280x720/scale 1, and `errors: []`; read-only postflight found an exact
packaged-process count of zero. The normal route is one passed route; association,
Explorer drag, and Jump List approval routes were skipped. Its screenshot was
visually inspected and shows the Unicode/spaces title, Saved state, authored Prompt
node, and File > Open result.

The aggregate Recent snapshot remained at 208 files (preflight digest
`147b2c54...94c7`; postflight digest `3c270dcb...11e8`). The pre-existing opaque
`CustomDestinations\\590aee7bdd69b59b.customDestinations-ms` changed from
`d739c7...fec2` to `0eee330...a6f` at `2026-08-03T02:44:29.8032683Z`; no restore,
delete, overwrite, or timestamp repair was attempted. The finalization attempt
passed with exact-process proof and no links removed. This cross-reference adds
supporting P evidence only; it intentionally does not change the A classifications,
promote the T01 ledger, or claim T03/Phase 0 acceptance.

## Audit conclusion

Automated coverage is strongest for deterministic storage, leases, publication
failure, hard-kill recovery, staged-provider reconciliation, and logical repair.
The remaining A limitations are the Windows Save dialog, Explorer/Jump List
shell activation, and cross-machine portability. These remain open for T03's
required packaged journey; this map intentionally does not promote any ledger
status or claim Phase 0.
