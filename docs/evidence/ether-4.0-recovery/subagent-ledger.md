# Ether 4.0 recovery subagent ledger

This ledger records delegated implementation and independent-review work. A row is not approval; the original review task owns the final product audit and release decision.

| ID | Model | Effort | Assignment | Commit | Findings | Disposition |
| --- | --- | --- | --- | --- | --- | --- |
| T01-ledger | Luna | xhigh | Build the 250-item machine-readable acceptance ledger, explicit RX baseline classification, deterministic validator, status report, and owner-route mappings. No product behavior edits. | `1e6879f..1c62613` | 250/250 source records; revised baseline counts are FAIL 19, MISSING 9, PRESENT-UNPROVEN 222; historical 14 remain PRESENT-UNPROVEN; `pnpm.cmd run evidence:validate` and `pnpm.cmd run evidence:validate:self-test` pass; candidate/gate checks enforce hashes, canonical journeys, fixture indicators, and RX reason binding. Main review’s RX triage and journey-contract findings are resolved. | Accepted by main worker after the review corrections in `1d78ef2`/`1c62613`; Phase 0 independent review remains separate and pending. |
| T02-A/B-journey-driver-and-baseline | Terra | xhigh | Reusable source-Electron/packaged journey driver plus rejected-candidate blank-authoring baseline: isolated profile, exact identity/hashes, human-readable action/error evidence, real pointer/keyboard/inline input, no-seed safety guard, and paired screenshots. No renderer or application behavior changes. | `47d03c4..e65f035`; evidence `6ad8ff7` | Driver exposes only Page-based authoring input, rejects seeded/bridge/database/injection declarations, records source/package artifact hashes and device scale, and scopes cleanup to its temp root. Failure cleanup now terminates only new processes from the exact unpacked executable path before falling back to its owned launcher. Contract test (5), testing-package TypeScript, and scoped ESLint passed. Source and packaged baseline each captured 20 sequential screenshots and reproduced BL-01/03/04/05; BL-02 changed from the rejected candidate (left marquee 1, Shift marquee 2), so the comparison commands intentionally returned non-zero after evidence capture. Both Module attempts visibly produced `TRAVERSAL_INVALID: GROUP_NODE_MISSING` and zero Module cards. | Evidence complete and visually reviewed by the assigned worker. This is a baseline record, not Phase 0 approval; independent PH0 review remains required. |
| T03-A-support | Luna | xhigh | Supporting A-only audit mapping all 37 `AC-A02-001..026` and `AC-A03-001..011` document lifecycle/recovery requirements to exact existing automated test files and test names. Owned `phase-0/document-lifecycle/automated-evidence-map.md` and this row only; no product behavior, shared ledger/generator, or practical journey harness changes. | pending | Focused Vitest runs: 14 files, 309 passed, 0 failed (including opt-in destructive chaos: 15/15). Map marks each item `A-PASS`, `A-PARTIAL`, or `A-GAP` and calls out Save-dialog/shell, cross-machine, compact-field, cache-deletion, and corruption-classification gaps. | Supporting evidence only; does not replace P/M evidence or close T03/Phase 0. Main worker must fill the commit after review. |
| T01-crash-evidence-map | Luna | xhigh | Correct the T01 deterministic-versus-visible evidence map for `AC-A03-001..011`, regenerate the machine ledger/report, and add validator/self-test guards against a blanket A+P default. No T03 practical evidence or product behavior changes. | `56572f8` | `AC-A03-001..006` and `AC-A03-009` map to A-only deterministic proof; visible recovery `AC-A03-007,008,010,011` map to A+P. Baseline validator and self-test pass, including mutations for broad defaults and wrong map entries; counts unchanged at FAIL 19/MISSING 9/PRESENT-UNPROVEN 222. | Support assignment accepted by main worker after focused review; does not close T03 or Phase 0. |
| PH0-review | Sol | high | Independent Phase 0 reviewer: operate the relevant application/evidence workflow first, then inspect T01 ledger, validator, and baseline report. | pending | Pending independent review. | Must review before Phase 0 gate decision; cannot self-approve. |

## Contract

Each implementation row records model, effort, owned scope, exact commit, findings, and disposition. Review rows are assigned before the gate and remain pending until the reviewer reports application-first findings. Status changes must be made by the integration owner after reviewing the returned diff; this file does not replace `ledger.json` evidence.
