# Ether 4.0 recovery subagent ledger

This ledger records delegated implementation and independent-review work. A row is not approval; the original review task owns the final product audit and release decision.

| ID | Model | Effort | Assignment | Commit | Findings | Disposition |
| --- | --- | --- | --- | --- | --- | --- |
| T01-ledger | Luna | xhigh | Build the 250-item machine-readable acceptance ledger, explicit RX baseline classification, deterministic validator, status report, and owner-route mappings. No product behavior edits. | `1e6879f..1c62613` | 250/250 source records; revised baseline counts are FAIL 19, MISSING 9, PRESENT-UNPROVEN 222; historical 14 remain PRESENT-UNPROVEN; `pnpm.cmd run evidence:validate` and `pnpm.cmd run evidence:validate:self-test` pass; candidate/gate checks enforce hashes, canonical journeys, fixture indicators, and RX reason binding. Main review’s RX triage and journey-contract findings are resolved. | Accepted by main worker after the review corrections in `1d78ef2`/`1c62613`; Phase 0 independent review remains separate and pending. |
| T02-A/B-journey-driver-and-baseline | Terra | xhigh | Reusable source-Electron/packaged journey driver plus rejected-candidate blank-authoring baseline: isolated profile, exact identity/hashes, human-readable action/error evidence, real pointer/keyboard/inline input, no-seed safety guard, and paired screenshots. No renderer or application behavior changes. | `47d03c4..e65f035`; evidence `6ad8ff7` | Driver exposes only Page-based authoring input, rejects seeded/bridge/database/injection declarations, records source/package artifact hashes and device scale, and scopes cleanup to its temp root. Failure cleanup now terminates only new processes from the exact unpacked executable path before falling back to its owned launcher. Contract test (5), testing-package TypeScript, and scoped ESLint passed. Source and packaged baseline each captured 20 sequential screenshots and reproduced BL-01/03/04/05; BL-02 changed from the rejected candidate (left marquee 1, Shift marquee 2), so the comparison commands intentionally returned non-zero after evidence capture. Both Module attempts visibly produced `TRAVERSAL_INVALID: GROUP_NODE_MISSING` and zero Module cards. | Evidence complete and visually reviewed by the assigned worker. This is a baseline record, not Phase 0 approval; independent PH0 review remains required. |
| T03-A-support | Luna | xhigh | Supporting A-only audit mapping all 37 `AC-A02-001..026` and `AC-A03-001..011` document lifecycle/recovery requirements to exact existing automated test files and test names. Owned `phase-0/document-lifecycle/automated-evidence-map.md` and this row only; no product behavior, shared ledger/generator, or practical journey harness changes. | `5a0f046` | Focused Vitest runs: 14 files, 309 passed, 0 failed (including opt-in destructive chaos: 15/15). Map marks each item `A-PASS`, `A-PARTIAL`, or `A-GAP` and calls out Save-dialog/shell, cross-machine, compact-field, cache-deletion, and corruption-classification gaps. | Supporting evidence only; accepted by the main worker as a current audit map, but it does not replace P/M evidence or close T03/Phase 0. |
| T03-A-gap-closure | Luna | xhigh | Close deterministic A02/A03 evidence gaps without product behavior changes: New/no-picker and AppData metadata placement, clean close/stale-journal writable reopen, Ctrl+S/history routing, successful-autosave quit, normative 12s location bound, recent/Jump List missing-target main route, field-by-field compact preservation, disposable-root deletion integrity, and distinct media-vs-graph repair UI grouping. Owned focused tests under `packages/testing/tests` (excluding Windows harnesses and packaged recovery driver) plus the automated evidence map and this row. | `69bb687` | Vitest lifecycle/vertical slice: 2 files, 89 passed, 0 failed; source-browser history/repair: 2 passed; source-Electron lifecycle: 12 passed; testing package typecheck and lint pass. Map now records exact test names/results and keeps native Save dialog, Explorer/Jump List shell activation, and cross-machine portability as packaged limitations. No product behavior changed. | Supporting A evidence only; pending main-worker review and required packaged/owner evidence. |
| T01-crash-evidence-map | Luna | xhigh | Correct the T01 deterministic-versus-visible evidence map for `AC-A03-001..011`, regenerate the machine ledger/report, and add validator/self-test guards against a blanket A+P default. No T03 practical evidence or product behavior changes. | `56572f8` | `AC-A03-001..006` and `AC-A03-009` map to A-only deterministic proof; visible recovery `AC-A03-007,008,010,011` map to A+P. Baseline validator and self-test pass, including mutations for broad defaults and wrong map entries; counts unchanged at FAIL 19/MISSING 9/PRESENT-UNPROVEN 222. | Support assignment accepted by main worker after focused review; does not close T03 or Phase 0. |
| T03-document-lifecycle | Terra | high | Map and exercise `AC-A02-001..026` and `AC-A03-001..011` document lifecycle/recovery through a blank UI-authored source-Electron and packaged-capable driver journey; preserve the `.ether` boundary and fail-closed behavior. | `7e4a26b..a824e69` | Supporting source evidence passed at `a824e69` for `document-lifecycle`, `document-lifecycle-reopen`, and `document-lifecycle-clean-reopen`, each with zero captured renderer errors and recorded artifact SHA-256s. Both reopened screenshots visibly show Saved, enabled Save, and Local document with no read-only banner. `63e1de6` reclaims a fresh same-machine lease only after its owner is proven dead and SQLite/mutex/reread safeguards pass; focused locking tests retain live and fresh-foreign fail-closed coverage. The journey now kills and verifies the exact browser-root process tree, then labels the hard-kill and Windows UI Automation clean-close cases accurately. It also visibly creates a blank graph, Ctrl+S saves, reviews the `Manual milestone: Manual save` History record, Save As/Copy/Compact/Portable, and exact-argv reopens. `29d1ee8` regression-tests argv bootstrap returning a descriptor rather than a schema-invalid boolean. The 37-requirement A map and requirement-level packaged coverage plan are supporting planning evidence, not P. | Pending main review; source Electron is diagnostic support only. Packaged Ether.exe P evidence, visible recovery-history P fixture, Explorer association/drop/Jump List, cache/staging/live-output deletion, corruption/repair, cross-machine and remaining map gaps are open. Package attempt previously timed out before artifacts; not Phase 0 completion. |
| T03-document-history | Terra | xhigh | Surface manual milestones and review-required recovered work through a typed, accessible Document History UI; persist staged recovery as a distinct revision. | `c803239` | Added `document.history`, a focus-trapped Project-header dialog, manual/autosave milestone display, current-head state, and explicit recovered-review metadata. Staged artifact/provider reconciliation now records a separate recovery revision. Focused integration 107/107, browser history 1/1, renderer production build, desktop lint, and later full desktop typecheck passed after the main worker corrected the independent shortcut guard defect. | Accepted by the main worker as T03 implementation support; packaged History and recovery evidence plus independent phase review remain open. |
| T03-repair-surface | Terra | high | Add the desktop-owned damaged-document repair flow for AC-A03-008, AC-A03-010, and AC-A03-011: native source/destination selection, AppData-rooted repair, opaque lossy confirmation, visible path-free report, and focused A coverage. | `5288461` | `repairDocument` now returns a non-publishing loss preview, rejects non-`.ether`, same, and existing destinations, and retains source-hash protection. Main process owns source/destination paths and a five-minute confirmation token; preload/IPC expose no paths. The persistent header command and modal classify media/artifact/blob losses separately from graph losses, trap focus, restore focus, and clear pending confirmation on cancel/Escape. Focused A: desktop IPC 16/16; recovery integration 24/24; desktop typecheck/lint; source-Electron desktop suite 11/11. | Candidate implementation only; does not close T03, Phase 0, or any P/M evidence. Packaged repair journey and independent review remain open. |
| T03-A02-windows-integration | Terra | high | Add a distinct approval-gated A02 Windows packaged-journey harness: isolated test roots/profiles, exact unpacked `Ether.exe`/PID scoping, real pointer/keyboard/UI Automation actions, native picker, representative one-file/identity/Save As/Copy/cache deletion checks, reversible Explorer association, actual Explorer pointer drag, and Jump List routes. No product behavior or ledger-status changes. | `f16f4239ebab0aa06114f2325a7c4c11f86ac379..5bf1e40a7c8a500e799b9812b5194969c8c946c4` | Package-independent contracts pass 4/4; focused ESLint and full contracts 41/41 pass. Normal packaged coverage is opt-in and never mutates the registry. Association and shell routes require distinct exact approval tokens. The drag route resolves only the unique test-folder Explorer HWND, moves it away from the exact Ether target, sends native pointer down/move/up, logs coordinates, and closes only that Explorer window. Jump List interaction refuses ambiguous taskbar/recent accessibility targets and records `JUMP_LIST_UNAVAILABLE` rather than falling back to argv; the missing-target path requires an exact Ether native error dialog. Every journey snapshots and removes only isolated-profile Recent shortcuts resolving to its unique test-owned documents. | Harness/contracts only; no package, association, drag, or Jump List shell route was run. `AC-A02-012` drag/drop and `AC-A02-016` Jump List remain open packaged-evidence gaps, as do removable/cloud variants. Association and shell routes remain unrun pending main authorization. |
| T03-native-dialog-diagnosis | Terra | high | Independently diagnose the packaged Save/Save As native-dialog failure without editing, shell mutation, or provider work; inspect the exact package, lifecycle queue, PID resolution, and cancellation semantics. | Main integration `7613d25` | Confirmed the package matched source and lifecycle serialization was not deadlocked. Identified the original PID/class-only UIA selector as a false-negative risk and found the independent untitled-save cancellation defect: a cancelled picker could still report Saved and create a manual milestone. Main investigation then proved the exact Ether owner HWND was disabled while Windows omitted the owned `#32770` dialog from UIA root children. | Findings accepted. Main integration now enumerates exact GW_OWNER handles, discovers controls through UIA, types/clicks only exact child HWNDs, returns early after cancelled untitled Save, and exposes dirty `Saving` state immediately. Focused lifecycle 90/90, Windows contracts 4/4, desktop/testing typechecks, and focused ESLint passed; exact packaged rerun follows package rebuild. |
| T03-Windows-shell-safety-review | Sol | high | Independently review the approval-gated association, Explorer, Jump List, recovery-AUMID, process/window targeting, restoration, retry, and cleanup-ordering design. Use read-only/static validation only; do not run package, shell UI, association mutation, COM, or cleanup routes. | Main integration `f2fe6c0..f3cdde34e6142e4fec8d51437ea80b003c0adb58` | Initial BLOCK found production-AUMID pollution, globally destructive Recent clearing, unsafe Jump List/Explorer targeting, incomplete association notification/effective-handler proof, watchdog crash gaps, and inability to distinguish absent/empty or preserve `REG_EXPAND_SZ`. An intermediate PASS was invalidated when app-scoped COM probes created real 2560-byte artifacts; exact artifacts were byte-verified and Recycled. Further corrections established exact packaged A02 Recent admission, retained owned-process absence before COM, retryable cleanup ordering, restoration-data preservation, and exact AutomaticDestinations proof. The PASS at `12207dc6972077505912e304139a1248ab70967b` was then invalidated by controlled packaged runtime evidence: the former parser converted newline-terminated query output's blank token through `Number("")` to PID `0`, repeatedly composed the recorded shutdown command, and failed proof closed; the native Save also changed a pre-existing real CustomDestinations file, which was preserved without speculative restoration. Static re-review then covered PID fix `e012f25ca08bfb61f9bb4ab79904189f2ae2b2c8`, S0/S1 redesign `8b98530151917c054e333693e373a8b4ffb8a067`, factual record `1ae966dd6c964e42375803d0d2f194d086c1ec9d`, and exact-link correction `70534f254155e3627d73ce7edde7deb802ce6f6d`. It verified strict positive-safe PID parsing before shutdown command construction without weakening retained owned-process absence or post-exit cleanup ordering; diagnostic S0 recording with stable S1/full-equality restoration for association and drag; and a two-session Jump route that completes ordinary Recent-disabled native Save and exact process exit before S1, proves zero exact-target links across real and isolated roots, removes only exact post-S1 links after exact process exit, requires the sole new AutomaticDestinations delta, then enforces same-file COM mutation, 2560-byte state, hash rechecks, Recycle Bin removal, and full S1 equality. Unproven process, association, or shell restoration preserves diagnostic roots. The PASS at `70534f254155e3627d73ce7edde7deb802ce6f6d` was invalidated by a second controlled packaged run: the primary lifecycle reached writer-lock setup, but the old contender userData basename was correctly rejected by the tightened resolver; no exact process/profile remained, the changed existing MRU was recorded and preserved, and failed evidence was excluded. Narrow static review covered contender fix `89292f180517a030879fb34dc0aeaedb3d9cea4f` and incident ledger `3385fa5411ae9db962c7c5d29d17254e4743ce7d`; the nested contender passes both disposable-profile gates, remains under the common owned root, has distinct userData/process markers and its own `4.0` directory with only `leases` junctioned to the primary lease root. Final review covered primary checkpoint `9ef6bfafd7a0ab67a45ea1f1e8757b45b6ce4f78`, process-proof correction `5002f6692dbe62b83bd65bbbb23fe65fa99208a7`, and post-checkpoint preservation `f3cdde34e6142e4fec8d51437ea80b003c0adb58`. It verified S1 is captured only after the final native File > Open title check and rechecked before second-instance, disposable-root, reopen, and association dry-run work; final cleanup requires exact S1 equality and proven session/process shutdown; pre-S1 failure cleanup still requires process proof; every post-S1 assertion/action failure preserves and reports the exact profile/root even when shutdown and equality pass. Primary remains ordinary recovery with no approval-gated action or registry mutation. The combined `f3cdde3` evidence commit did not alter the reviewed code scope beyond the three expected harness files. Desktop/testing TypeScript, focused ESLint, committed/worktree diff checks, and 20/20 safe static contracts passed. | PASS-to-run at exact HEAD `f3cdde34e6142e4fec8d51437ea80b003c0adb58`. This authorizes the normal controlled A02 primary route and separately approved practical shell routes only; it is not T03 or Phase 0 acceptance. No package, shell UI, association mutation, COM, or cleanup route ran during this final review. |
| T03-packaged-lifecycle-runtime-incident | Terra | high | Diagnose the controlled normal packaged `document-lifecycle` failure at source commit `6545bd9`, preserve all affected state, and correct only the harness PID parser. No approval-gated route rerun. | `e012f25ca08bfb61f9bb4ab79904189f2ae2b2c8` | The process query emitted PID `0`; the wrapper then safely detected the invalid `Stop-Process -Id 41408,36540,40364,38536,0 -Force -ErrorAction SilentlyContinue` shutdown attempt and failed the journey. Read-only inspection found no exact packaged Fixer `Ether.exe` process remaining. The isolated profile and changed pre-existing `CustomDestinations\\590aee7bdd69b59b.customDestinations-ms` are preserved with exact timestamps/hashes in the shell-state incident. The correction exports a strict positive-safe PID parser that rejects zero, negative, non-numeric, mixed, and non-safe output before a command is composed; focused driver contracts 9/9, Windows contracts 8/8, testing/desktop TypeScript, and focused ESLint pass. | Runtime finding recorded; failed package evidence is excluded. A fresh controlled packaged rerun remains separately authorized work. |
| T03-S0-S1-shell-baseline-correction | Terra | high | Correct the packaged lifecycle incident facts and redesign approved association, Explorer drag, and Jump List shell proof around an S0 diagnostic and S1 restoration checkpoint. Static-only; no approval route rerun. | `8b98530151917c054e333693e373a8b4ffb8a067` | Factual correction to the preceding runtime row: the Windows query did not emit PID `0`; a trailing newline blank became `Number("") === 0` in the former parser, so the driver repeatedly composed the recorded command and failed shutdown proof closed. S0→S1 setup deltas are now recorded without a restore claim; every post-S1 delta fails closed. Jump List uses an ordinary recovery native-save setup session that exits before S1, then an exact approved Recent-mode session that opens the existing document without native Save and must prove one post-S1 recovery AutomaticDestinations delta before same-file COM/hash/recycle return to S1. Static contracts: Windows 9/9, driver 9/9; testing/desktop TypeScript and focused ESLint pass. | Supersedes the prior row's incorrect `query emitted PID 0` wording. Failed packaged evidence remains excluded; a fresh controlled packaged run remains separately authorized work. |
| T03-writer-lock-contender-profile-correction | Terra | high | Diagnose the second controlled packaged lifecycle failure at the writer-lock contender boundary, preserve affected shell state, and align the nested contender profile with reusable-profile and recovery-shell resolver rules. Static-only; no rerun. | `89292f180517a030879fb34dc0aeaedb3d9cea4f` | At HEAD `78b062ffe2d21012b5bae89cd22088a39ee23cf0`, the primary UI/native lifecycle completed setup but the contender used `<primaryRoot>\\contender-user-data`, which the resolver rejected because its basename was not `Ether-Recovery-Profile`. The corrected non-colliding path is `<primaryRoot>\\contender\\Ether-Recovery-Profile`, with its own `4.0\\leases` junction to the primary lease. The reusable-profile guard and resolver contracts cover nested valid versus invalid basenames; driver contracts 9/9, Windows contracts 10/10, testing/desktop TypeScript, and focused ESLint pass. No exact Fixer process remained; driver cleanup succeeded. The changed existing MRU file is preserved and the failed evidence is excluded. | Runtime finding recorded and harness corrected; a fresh controlled packaged rerun remains separately authorized work. |
| T03-primary-S0-S1-shell-checkpoint | Terra | high | Bring the normal A02 primary packaged route under the S0/S1 shell-baseline model before another controlled run. Static-only; association remains dry-run only. | `9ef6bfafd7a0ab67a45ea1f1e8757b45b6ce4f78` | The route now records S0 before launch and a full S0→S1 opaque setup delta only after final native File > Open title verification, then immediately rechecks S1. Second-instance focus, disposable-root work, close/reopen, and association dry-run are post-S1 and require exact final S1 equality. Any post-S1 session/process/equality failure preserves the exact primary profile and Windows root with named paths; a pre-S1 failure may clean scoped artifacts while retaining finalization errors. Windows contracts 11/11, driver contracts 9/9, testing/desktop TypeScript, and focused ESLint pass. | Harness safety correction only; no runtime, approval route, or association mutation ran. |
| T03-packaged-evidence-reconciliation | Luna | xhigh | Reconcile the successful controlled packaged lifecycle and visible-recovery evidence at HEAD `5417ce1`; visually inspect retained screenshots/action logs, record exact package identity and shell-state deltas, and update only T03 evidence/coverage docs. No product, harness, association, Explorer, Jump List, or owner-route changes. | Evidence run `5417ce1`; integrated documentation `f3cdde34e6142e4fec8d51437ea80b003c0adb58` | Four lifecycle journeys and six visible-recovery records passed with fresh isolated profiles, 1280x720/scale-1, shared `Ether.exe`/`app.asar` hashes, zero captured errors, and no exact packaged Fixer process at postcheck. Lifecycle evidence visibly covers native Save, one-file publication, History, Save As/Copy/Compact/Portable, autosave, hard-kill reopen, clean UIA close/reopen, and competing-writer read-only state. Visible recovery evidence covers review-required provider recovery, unsupported metadata, and distinct media/artifact versus graph repair losses. Native Save changed the pre-existing `CustomDestinations\\590aee7bdd69b59b.customDestinations-ms` from `6ae9b25b...` to `bac64f8...`, then from `bac64f8...` to `73f204b...`; both opaque deltas are recorded with no restore/delete/overwrite attempt. | Accepted into exact integration commit `f3cdde34e6142e4fec8d51437ea80b003c0adb58` as bounded packaged support only. Association, Explorer drag, Jump List, cross-machine/long-path/removable/cloud variants, and owner-M requirements remain open; no T03 or Phase 0 acceptance is claimed. |
| T03-shared-shell-classifier-and-J0-baseline | Terra | high | Static-only recovery from the excluded `bdc3d76` combined A02 attempt: add shared shell-delta classification, explicit exact-target-link checkpoints, and the Jump List J0/J1/J2 micro-baseline. No package, UI, registry, COM, or cleanup route rerun. | `2107412` | The classifier permits only in-place pre-existing Automatic/CustomDestinations changes for reporting, rejects every removal/non-opaque change/unexpected new file, and never restores opaque shell files. Primary, association, and drag record S0→final/S1→final action evidence only after exact process exit and exact-link cleanup; every post-S1 failure preserves root/profile. Jump requires exactly one new recovery candidate at J0, exact-candidate-only COM mutation with 2560-byte/hash proof at J1, and J2=J0 minus that candidate. Contracts 14/14; focused ESLint; testing and desktop TypeScript pass. The preserved `8ILUcl`/`FMILz4` failed artifacts are documented but untouched. | Static change-control correction only. A fresh VM/Sandbox controlled run is future authorized work; no packaged/approval-route evidence or Phase 0 acceptance is claimed. |
| T03-post-exit-shell-sanitization-review-fixes | Terra | high | Static-only correction of Sol's P0 findings against `2107412/d41f53b`: decouple real-shell sanitation from diagnostic-root retention, harden `.lnk` deletion proof, cover no-session post-S1 failure, and make Jump cleanup resume from stored J0/J1 state. No package, UI, registry, COM, or cleanup route rerun. | `093dc57` | After an exact shutdown proof, S1 sanitation is attempted even when the journey failed; only disposable profile/root deletion remains vetoed. Every `.lnk` candidate must be absent from the full S1 root, then retain identical pathname/size/SHA-256 and re-resolve to the exact random document immediately before deletion. Launch-failure callbacks run only after driver shutdown proof and block driver profile cleanup if they fail. Primary has an explicit no-session process-absence proof/fallback; Jump stores J0/candidate/J1/stage and distinguishes COM not-applied/applied plus candidate-present/recycled retries. Static contracts 25/25, focused ESLint, testing and desktop TypeScript pass. `8ILUcl`/`FMILz4` remain untouched. | Static corrective implementation only; fresh VM/Sandbox controlled evidence and independent re-review remain required. No acceptance claim. |
| T03-exact-process-proof-and-finalization-sidecar | Terra | high | Static-only follow-up to the second Sol P0/P1 review: require route-level exact process-absence proof before artifact deletion and create durable per-journey shell-finalization sidecars. No package, UI, registry, COM, or cleanup route rerun. | `f700612` | Primary, association, Explorer drag, and both Jump sessions reset `exactProcessAbsenceProven` before launch; only driver post-shutdown callbacks or after-exit hooks set it. Artifact deletion now separately requires that flag and failure-free finalization. Each committed journey initializes `shell-finalization.json` before launch; sanitation/retry/no-session attempts record identity, proof context, outcome, classifier disposition/failure, candidate, and Jump stage. The recorder refuses unproven sanitation and writes a blocked record when S1/proof is absent; sidecar-write failure fails closed. Static contracts 25/25, focused ESLint, testing and desktop TypeScript pass. Preserved A02 evidence remains untouched. | Static evidence-control correction only; independent re-review and a separately authorized fresh VM/Sandbox run remain required. No acceptance claim. |
| T03-finalization-sidecar-durability-and-identity | Terra | high | Static-only follow-up to Sol's final P0 safety findings: make every sidecar write history an irreversible cleanup veto, require complete packaged identity before sidecar creation, and record every internal Jump COM/recycle retry. No package, UI, registry, COM, or cleanup route rerun. | `3c0bbef` | All four routes retry their full current sidecar before cleanup; any current or historical write failure becomes a named finalization failure and prevents artifact deletion. Association/drag now append a generic blocked record even when a session was acquired but S1 never was. Sidecars require a 40-hex commit plus 64-hex `Ether.exe` and `app.asar` hashes before launch. Jump attempts persist invocation counters and observed COM/recycle states, including no-effect and candidate-present retries. Static contracts 26/26, focused ESLint, testing/desktop TypeScript, and diff checks pass. Preserved A02 evidence remains untouched. | Static evidence-control correction only; independent re-review and a separately authorized fresh VM/Sandbox run remain required. No acceptance claim. |
| T03-final-shell-safety-review | Sol | high | Independently re-review exact HEAD `5c1bc15` / code commit `3c0bbef` as the static PASS-to-run gate for Windows shell/process sanitation. No Electron, COM, registry, shell UI, cleanup, or evidence route was executed. | `5c1bc15e9a0991c647be485ab16939065928e306` | PASS-to-run. Every initial/reopen/recent launch resets explicit process proof and supplies a driver-confirmed launch-failure callback; every close proof is set only from the driver's post-shutdown hook. Diagnostic cleanup requires exact absence, zero finalization failures, durable sidecar state, no post-S1 journey failure, and restored S1 when captured. Sidecars exist before launch with exact commit plus `Ether.exe`/`app.asar` hashes, record passed/failed/blocked/retry dispositions, and permanently veto cleanup after any write failure. Full-S1 `.lnk` absence plus byte/target rechecks, narrow opaque classifier allowlists, and retryable Jump J0/COM/J1/recycle/J2 transitions remain fail-closed. Testing and desktop TypeScript, focused ESLint, 26/26 contracts, and diff checks passed; preserved A02 evidence was untouched. | Independent static approval for a separately authorized fresh VM/Sandbox controlled run only. No Electron/approval route ran and no T03, Phase 0, or release acceptance is claimed. |
| T03-A02-enametoolong-sanitization | Luna | xhigh | Correct the reviewed A02 packaged cleanup failure without running Electron, shell UI, COM, registry mutation, or cleanup: remove full S1 path-set command embedding, enumerate exact-target `.lnk` candidates read-only per Recent root, validate root-relative paths in Node, prove absence against the full S1 snapshot, and delete each candidate through a short per-candidate proof script. Own `windowsIntegration.ts`, focused contracts, shell-state incident, and this row. | `1a5f389` | The controlled run failed safely at after-exit link sanitation with `spawn ENAMETOOLONG`, despite zero exact packaged processes remaining. The correction validates every enumerated candidate as an exact top-level `.lnk` under its real/isolated Recent root, rejects duplicate absolute candidates, calls full-S1 absence classification before any deletion, then rechecks exact pathname/positive-safe size/SHA-256/target under the random root before deleting one file and proving absence. Traversal, non-shortcut, duplicate, and S1-pre-existing paths remain fail-closed. Incident records root/profile, aggregate Recent digests, and the pre-existing MRU SHA/timestamp without restoration. | Static correction only; packaged evidence remains excluded and association/Explorer/Jump List routes remain separately gated. |
| T03-A02-enametoolong-static-review | Sol | high | Independently re-review exact HEAD `7f5e170` and code commits `0bfd8aa`/`1a5f389` as the static PASS-to-run gate after the packaged `spawn ENAMETOOLONG` failure. No Electron, COM, registry, shell UI, cleanup, or preserved evidence route was executed. | `7f5e1708a8912e1580ea8bc6b15b94dcfb857a77` | PASS-to-run for the separately authorized fresh packaged normal route. No PowerShell command embeds the full S1 path set. Discovery is read-only, top-level Recent only, and exact-target/random-root scoped; Node derives exact root-relative `.lnk` candidates, rejects duplicate absolute candidates, and proves every candidate absent from the complete S1 snapshot before deletion begins. Each candidate then uses one bounded command that rechecks exact pathname, stable positive-size/SHA-256 evidence, exact target twice, random-root containment, and final absence; Node rejects malformed size/hash/path/target proof. Existing exact-process, finalization-failure, durable-sidecar, classifier, and profile/root cleanup gates remain intact. Testing and desktop TypeScript, focused ESLint, 27/27 focused contracts, and diff checks passed; preserved A02 evidence remained untouched. | Independent static approval only for a separately authorized fresh packaged normal route. Association, Explorer drag, Jump List, T03, Phase 0, and release acceptance remain open. |
| T03-association-powershell51-static-review | Sol | high | Independently review exact code commit `9386d2252b88d40163c35265fa70387256afc2c6` as the narrow static gate after the excluded association route failed on ambiguous Windows PowerShell 5.1 `Split-Path` binding. No Electron, Explorer, COM, registry, shell UI, cleanup, or evidence route was executed. | `9386d2252b88d40163c35265fa70387256afc2c6` | PASS-to-run for one separately authorized association retry. Both Explorer helpers now derive the canonical parent with `System.IO.Path.GetDirectoryName(GetFullPath(...))`; the ambiguous `Split-Path` command is absent from generated scripts. The exact full document path remains PowerShell-single-quote escaped, drives Explorer `/select`, and yields only its leaf name. Targeting still requires a newly created Explorer HWND at the exact canonical folder, one unique matching item, the exact packaged Ether PID/window, and exact foreground activation. The inner `finally`, outer restoration retry, detached watchdog, exact-process proof, durable sidecar, shell classifier, and diagnostic cleanup vetoes remain unchanged. Testing and desktop TypeScript, focused ESLint, 27/27 focused contracts, and exact-code/worktree diff checks passed. | Independent static approval only for one separately authorized association retry. Explorer drag, Jump List, T03, Phase 0, and release acceptance remain open. |
| T03-Windows-explorer-path-safety | Terra | high | Correct the excluded `14dda6c` association/Explorer drag PowerShell 5.1 folder-derivation failure by using safe exact folder derivation in both Explorer association and drag helpers. No product behavior or approval-route rerun. | `9386d22` | Replaced ambiguous `Split-Path` use with PowerShell 5.1-safe exact parent-folder derivation in both helpers; 16/16 Windows contracts, testing/desktop TypeScript, focused ESLint, and diff checks passed. The archived failed association evidence remains untouched. | Static implementation support only; retry still requires an independent Sol gate and separate approval. |
| T03-A02-normal-packaged-evidence | Luna | xhigh | Reconcile the successful normal A02 packaged route at exact evidence identity `14dda6c0aa33398bfdb4424ad21580d67ffd44b1`; inspect retained screenshot, action logs, results, and finalization sidecars; record package identity, process absence, Recent snapshots, and opaque MRU change. Own the two canonical normal evidence directories plus T03 coverage docs and shell incident only; no product, harness, shell UI, COM, registry, cleanup, or approval-route actions. | `ecacb76` (evidence) | Primary and reopen records pass with the exact `Ether.exe`/`app.asar` hashes, fresh isolated profiles, 1280x720/scale-1, and exact packaged-process count 0. The normal route is one passed route; association, Explorer drag, and Jump List approval routes were skipped. Preflight/postflight Recent remain 208/208 with digests `147b2c54...94c7`/`3c270dcb...11e8`; pre-existing `590aee7bdd69b59b.customDestinations-ms` changed from `d739c7...fec2` to `0eee330...a6f` at `2026-08-03T02:44:29.8032683Z` and was preserved without restore/delete/overwrite. Finalization passed with exact-process proof and no links removed. Screenshot and all sidecars/logs/results were read; coverage docs retain open approval/owner boundaries. | Bounded packaged evidence only; no T03, Phase 0, or release acceptance claim. |
| T03-association-hidden-extension-static-review | Sol | high | Independently review exact code commit `e7bda3a36cd55e3b2c3ae0229ba931acf0198f91` as the security/targeting gate after the excluded hidden-extension association attempt. No Electron, Explorer, COM, registry, shell UI, cleanup, or evidence route was executed. | `e7bda3a36cd55e3b2c3ae0229ba931acf0198f91` | PASS-to-run for one separately authorized association rerun. Both Explorer helpers normalize the exact full document, require Shell `NameSpace` for its exact folder and `ParseName` for its exact leaf, reject any parsed-path mismatch, and use only the nonempty Shell display name for UIA. A unique new Explorer HWND must match the exact folder; polling and every pre-action recheck require one UIA display candidate plus exactly one COM-selected item whose `Item(0).Path` equals the full document. Invoke rechecks immediately before activation; drag rechecks before and after arranging its window while retaining exact Ether PID/window/target bounds. The shared PowerShell runner computes Base64 length from UTF-16LE bytes and refuses commands above 30,000 characters, leaving CreateProcess headroom; unchanged direct encoded scripts remain bounded. Restoration/watchdog, exact-process, durable-sidecar, classifier, and cleanup vetoes remain intact. Testing and desktop TypeScript, focused ESLint, 28/28 focused contracts, and diff checks passed. | Independent static approval only for one separately authorized association rerun. Explorer drag, Jump List, T03, Phase 0, and release acceptance remain open. |
| T03-association-foreground-transition-static-review | Sol | high | Independently review exact code commit `329252305127b09424af721300ffa021c85d87b9` as the security/targeting gate for same-interaction Explorer-to-Ether foreground-transition proof. No Electron, Explorer, COM, registry, shell UI, cleanup, or evidence route was executed. | `329252305127b09424af721300ffa021c85d87b9` | FAIL. The association and drag discovery loops place the deliberate zero-PID rejection inside a broad `try { ... } catch {}`, swallowing `Exact Explorer HWND resolved to a non-positive PID` instead of failing immediately. More importantly, `exactExplorerForegroundScript` verifies `SetForegroundWindow` and `GetForegroundWindow` for the stored HWND but never re-runs `GetWindowThreadProcessId` and compares it with the stored nonzero Explorer PID immediately before Invoke/native pointer input, so the action boundary does not prove the foreground HWND still has the resolved Explorer identity. The exact Ether HWND/PID resolution, association minimized-state check, exact selected-path rechecks, same-script Ether HWND/PID transition polling and latency output, route-local removal of delayed focus snapshots, retained header/process assertions, 30,000-character encoded-command guard, and unchanged restoration/sidecar/classifier/cleanup gates otherwise satisfy the reviewed design. Testing and desktop TypeScript, focused ESLint, 30/30 focused tests including the generated drag-command ceiling, and diff checks passed. | No association rerun is authorized. Move the zero-PID assertion outside the tolerant enumeration catch and revalidate the exact Explorer HWND-to-PID binding at the final foreground/action boundary, then obtain a fresh independent static gate. Explorer drag, Jump List, T03, Phase 0, and release acceptance remain open. |
| T03-association-foreground-transition-correction-review | Sol | high | Re-review exact correction commit `2e3f054916698e1e96767e4c3c84d49c11a3439c` after the foreground-transition FAIL. Static validation only; no Electron, Explorer, COM, registry, shell UI, cleanup, or evidence route was executed. | `2e3f054916698e1e96767e4c3c84d49c11a3439c` | FAIL. The correction successfully moves positive Explorer PID resolution outside tolerant enumeration catches and makes the foreground helper require a nonzero current PID equal to the stored Explorer PID after exact `SetForegroundWindow`/`GetForegroundWindow`. However, that final source-identity proof is not immediately adjacent to either action: association subsequently performs the COM selected-path recheck and Ether `IsIconic` call before Invoke, while drag subsequently calls `SetCursorPos` and waits 100 ms before mouse-down. Foreground or HWND ownership can therefore change after the last source proof but before the action, weakening the claimed exact Explorer-to-Ether transition. All previously passed exact selection, Ether HWND/PID, minimized-target, same-script transition/latency, route-local delayed-check removal, command-length, restoration, sidecar, classifier, and cleanup gates remain intact. Testing and desktop TypeScript, focused ESLint, 30/30 focused tests, and committed/worktree diff checks passed. | No association rerun is authorized. Complete all selection/minimized/pointer-position preparation first, then place exact Explorer foreground plus final nonzero/equal PID proof immediately before Invoke or mouse-down; add an ordering contract and obtain a fresh independent static gate. Explorer drag, Jump List, T03, Phase 0, and release acceptance remain open. |
| T03-association-foreground-transition-final-review | Sol | high | Final static adjacency re-review of exact commit `e563cb94c940563089709b90a282ee5ad80150d8` after both foreground-transition correction gates. No Electron, Explorer, COM, registry, shell UI, cleanup, or evidence route was executed. | `e563cb94c940563089709b90a282ee5ad80150d8` | PASS-to-run for one separately authorized association rerun. Association now completes the exact selected-path and minimized-target checks before the final Explorer `SetForegroundWindow`/exact foreground HWND/nonzero stored-PID equality proof, with only the activation timestamp before UIA Invoke. Drag positions and settles the cursor before the same final HWND/PID proof, with only the timestamp before mouse-down. Both routes retain positive PID resolution outside tolerant enumeration catches, exact Shell path/display identity, unique selected-item proof, one exact nonzero Ether HWND/PID, same-script exact Ether foreground transition polling, recorded source/target HWNDs/PIDs and latency, and no harness-forced Ether foreground. Generated-script ordering contracts exercise the actual builders; the drag command remains under the global 30,000-character encoded-command ceiling. Delayed focus snapshots remain removed only from association/drag while header and packaged-process assertions remain. Restoration/watchdog, durable-sidecar, classifier, process-absence, and cleanup vetoes are unchanged. Testing and desktop TypeScript, focused ESLint, 31/31 focused tests, and committed/worktree diff checks passed. | Independent static approval only for one separately authorized association rerun. Explorer drag, Jump List, T03, Phase 0, and release acceptance remain open. |
| T03-association-native-pointer-static-review | Sol | high | Fresh static PASS-to-run review of exact HEAD `128f7cd9f6e0e3dc70bb64ff5b0434029834e172`, including native Explorer foreground changes in `45df1ab`. No Ether, Explorer, registry, COM, PowerShell native UI, cleanup, or approval-gated route was run. | `128f7cd9f6e0e3dc70bb64ff5b0434029834e172` | FAIL. Both approval tokens are required before the association route, mutation helper, and Explorer helper; neither association nor drag contains `SetForegroundWindow`; exact HWND/PID/selection/minimized-target/transition ordering, mouse-release `finally` guards, drag `0x0054` NOACTIVATE placement, target root proof, command ceiling, and restoration/watchdog/sidecar/classifier/cleanup gates remain intact. The current native input targeting is not fail-closed, however: association and drag discard every `SetCursorPos` result and never call `GetCursorPos`, while `WindowFromPoint` validates the intended coordinates rather than the actual cursor position used by `mouse_event`. A failed move or concurrent cursor movement can therefore click or release over an unrelated window. Association's `ClientToScreen` check proves `+2,+2` is above the client origin, but “non-client” does not exclude non-client controls such as the system-menu/caption region on maximized or borderless frame states; `WindowFromPoint` plus `GA_ROOT` still returns the Explorer root for those controls. Current `POINT`, `ClientToScreen`, `WindowFromPoint`, and `GetAncestor` C# signatures are PowerShell 5.1-compatible. Representative generated scripts remain below the 30,000 global limit (the reviewed long-path sample generated 28,056/28,416 characters; the stated 28,064/28,424 runtime variants are also bounded). Testing and desktop TypeScript, focused ESLint, 31/31 focused tests, and committed/worktree diff checks passed. | No association attempt is authorized. Require successful `SetCursorPos`, verify exact actual coordinates with `GetCursorPos`, and run `WindowFromPoint`/`GA_ROOT` on that actual point immediately before down/up. For association, additionally use `WM_NCHITTEST` or an equivalent exact whitelist to accept only inert caption/resize-frame codes and reject client/system-menu/minimize/maximize/close controls. Apply equivalent actual-source/threshold/final-target cursor checks to drag, add generated-ordering contracts, and obtain a fresh independent static gate. Explorer drag, Jump List, T03, Phase 0, and release acceptance remain open. |
| T03-association-native-pointer-correction-review | Sol | high | Second fresh static PASS-to-run review of exact HEAD `a1a971e0506b54f03763932beec5573a7fb7b7c4` after pointer-targeting fixes in `f997077`/`a1a971e`. No Ether, Explorer, registry, COM, PowerShell native UI, cleanup, or approval-gated route was run. | `a1a971e0506b54f03763932beec5573a7fb7b7c4` | PASS-to-run for one separately authorized association attempt. Generated association code requires successful cursor placement, exact `GetCursorPos` equality, and `WindowFromPoint`/`GA_ROOT` equality for the actual point before mouse-down. Its click is at the left frame midpoint, must remain left of the `ClientToScreen(0,0)` client origin, and must return only `WM_NCHITTEST` resize-border values 10–17; client, system-menu, minimize, maximize, close, and every other hit code fail closed. Generated drag code applies successful placement/readback/root proof to the exact source, threshold move, and final Ether target, requires every progression move to succeed, proves Explorer foreground/PID while held, and verifies the actual Ether point immediately before release. Both retain NOACTIVATE placement, no `SetForegroundWindow`, guaranteed release, exact Shell/UIA/COM selection and PID identity, minimized association target, final-adjacent Invoke, exact Ether transition, both approval gates, and unchanged restoration/watchdog/sidecar/classifier/cleanup gates. `POINT`, `GetCursorPos(out POINT)`, `ClientToScreen(ref POINT)`, `WindowFromPoint(POINT)`, `GetAncestor`, `SendMessage`, `[ref]`, `-in`, and `-notin` are compatible with Windows PowerShell 5.1 for these positive primary-screen coordinates. Realistic generated sizes 27,876 association and 28,968 drag remain within the global 30,000 encoded-command limit. Testing and desktop TypeScript, focused ESLint, 31/31 focused tests, direct generated-command inspection, and committed/worktree diff checks passed. | Independent static approval only for one separately authorized association attempt. Explorer drag remains unrun and requires separate authorization after association evidence; Jump List, T03, Phase 0, and release acceptance remain open. |
| T03-A02-argument-forwarding-multi-route-archive | Luna | xhigh | Archive the accidental broad Playwright run at exact HEAD `9d14f93d973db59d6a90d116b7e849ddb084b3ae`; preserve overwritten normal evidence and association/Explorer-drag/Jump-List diagnostics; restore only the seven canonical normal files from `HEAD`; record shell/process safety facts. No Ether, Explorer, registry, COM, PowerShell UI, cleanup, harness, or product action. | `089a2e0` | The literal `--` in the intended association-only `pnpm ... run ... -- --grep ...` reached Playwright, so all four tests ran. Normal primary/reopen individually passed but were excluded and archived as overwritten; association failed exact Explorer-to-Ether foreground transition; Explorer drag timed out at 150 s; Jump List setup passed but the route failed with zero exact taskbar items and no new AutomaticDestinations candidate. All route roots/profiles and encoded diagnostics are preserved. Pre/post exact packaged process count was 0; registry digest stayed `e28e10c3...01c4`; Recent remained 209 files while its aggregate digest changed `bf49df40...cd62` -> `f9d7cb46...e34f`; the opaque `590aee7bdd69b59b.customDestinations-ms` changed in place and was never restored/deleted/overwritten. A read-only post-timeout probe found left mouse button not down; three long-lived encoded PowerShell hosts contained none of `EtherA02Pointer`, `EtherA02Native`, or `EtherA02JumpList`, and no process was killed or altered. Seven canonical normal files match `HEAD`; no route acceptance is claimed. | Evidence archive and incident record only; no A02/T03/Phase 0/release acceptance. Association, Explorer drag, Jump List, and shell restoration remain open. |
| T03-A02-d591fb8-association-native-enter-failure-archive | Luna | xhigh | Archive the single failed corrected association selector run at exact commit `d591fb84d6bf2790bea0f9408ccf6b95569b2fa4`; preserve the untracked canonical association evidence, exact shell/process observations, and external root/profile diagnostic references. No product/testing edits, rerun, UI/native route, shell mutation, or temp-root cleanup. | `0d2937c` | Approval-free selector `7a13ecc` failed safely with 0 tests after a Playwright full-title prefix; corrected `d591fb8` selected exactly one test/one file. Sol PASS-to-run used one worker, retries=0, exact direct selector, route=association, and only mutation+shell approvals; no retry. The one 38.5-second runtime failed `association Enter: foreground did not transition from the exact Explorer HWND to the exact Ether HWND/PID`; finalization proved exact process absence, passed shell classification, and restored registry SHA `e28e10c3...01c4`. Pre/post exact process 0, left-button high bit 0, retained route PowerShell 0, same Desktop HWND 131474/PID 16776, Recent 209 with aggregate `230cd9...12e7b` -> `32702a...7d3`, opaque 590a changed in place (`5a35...56f69` -> `af3d...f725`) and was never restored/deleted/overwritten. Disposable root/profile and critical registry/document/log diagnostics remain untouched; no observable second-instance/open/focus log is a diagnostic absence, not a root-cause claim. | Evidence archive and shell incident only; no association acceptance, T03, Phase 0, or release claim. |
| T03-association-9532736-terra-failure-diagnosis | Terra | high | Read-only diagnosis of the single approval-gated association attempt at exact HEAD `95327367b94357c9888aa350c717ae959245ebaa`; preserve the failure boundary and shell/process observations without a route rerun or source change. | — (no commit) | Exact selector ran one test in one file with one worker, `retries=0`, both required approvals, and runtime/command durations of 22.1/24.5 s. The attempt failed before native `SendInput` because Windows PowerShell 5.1 could not marshal the `RuntimeType` supplied to `Marshal.SizeOf`; no association diagnostic was emitted, so no foreground or product root cause is claimed. | Diagnosis complete; excluded failed packaged evidence only. |
| T03-association-9532736-sol-design-diagnosis | Sol | high | Independently assess the association activation/diagnostic design around the pre-`SendInput` failure, with no Electron, shell, registry, COM, cleanup, or approval-route execution. | — (no commit) | Design review kept the ABI-boundary exception separate from any association or foreground claim, required redacted correlation/activation diagnostics, and preserved fail-closed no-retry behavior when the route stops before Enter. | Design diagnosis complete; no implementation or route authorization. |
| T03-association-diagnostics-correction-arc | Terra | high | Implement the recovery-association diagnostics correction arc across `02e3a58`, `d9a6871`, and `9532736`; no approval-gated route rerun. | `02e3a58`, `d9a6871`, `9532736` | `02e3a58` added correlation-bound second-instance, focus, handled, and failure tracing; `d9a6871` preserved open-document return contracts; `9532736` deferred diagnostics so tracing cannot disrupt activation. The subsequent packaged attempt still failed before `SendInput` at the PowerShell `Marshal.SizeOf` boundary and remains excluded. | Diagnostics implementation arc recorded; no product acceptance or route pass claimed. |
| T03-association-native-enter-proof | Luna | xhigh | Harden the native association Enter proof and generated-command contracts at exact commit `1dd3957`; no shell route rerun or product behavior change. | `1dd3957` | Native ABI/input proof and focused generated-command contracts were hardened, but the later packaged route failed before the first native input call, so this proof does not convert the excluded run into association evidence. | Implementation support complete; packaged association evidence remains excluded. |
| T03-association-9532736-static-review | Sol | high | Independently review the association diagnostics/proof sequence across exact commits `02e3a58`, `1dd3957`, and `9532736`; static-only, with no route execution. | `02e3a58` -> `1dd3957` -> `9532736` | Review history: FAIL on the diagnostics API at `02e3a58`; FAIL on pre-focus `fsync` ordering at `1dd3957`; PASS-to-run at exact `9532736` after the diagnostics were deferred and the reviewed contract matched the intended activation boundary. | Final independent static gate recorded; it authorized one controlled attempt only, which subsequently failed safely and was archived. |
| T03-association-9532736-archive | Luna | xhigh | Archive the failed association attempt at exact HEAD `95327367b94357c9888aa350c717ae959245ebaa`; preserve canonical logs/results/sidecars, package hashes, shell-state facts, diagnostic root tokens, and the explicit no-retry boundary. Docs/evidence only. | `18d4a17` | Moved the canonical association evidence intact into `failed-attempts/2026-08-03-9532736-association-marshal-sizeof-type/`; added README, three-file SHA-256 manifest, and shell-state incident. Manifest hashes are stable, JSON evidence is retained, and no opaque shell file or external disposable root was touched. | Evidence archive complete; no association acceptance, T03, Phase 0, or release claim. |
| T03-association-marshal-sizeof-correction | Terra | high | Correct the PowerShell 5.1 ABI-size boundary used by the approval-gated association harness and add a non-UI runtime contract for the native `INPUT` layout. Owned `windowsIntegration.ts` and its contract test only; no association rerun. | `359ae89` | Replaced fragile `Marshal.SizeOf([RuntimeType])` invocation with a compiled native helper and asserted x64 size 40/x86 size 28. Focused Windows contracts 25/25, testing TypeScript, and focused ESLint passed. | Integrated by main worker; the prior failed packaged association evidence remains excluded and no route authorization is claimed. |
| T03-association-9532736-digest-archive-correction | Luna | xhigh | Expand the archived A02 association incident with exact digest/manifests and corrected shell-state facts at the failed-attempt boundary. Docs/evidence only; no route rerun or external-state mutation. | `df13e8c` | Updated the incident README, manifest, and shell-state record with stable exact hashes and preserved opaque-shell diagnostics. | Integrated; archive evidence remains diagnostic only and does not establish association, T03, Phase 0, or release acceptance. |
| T03-association-native-abi-independent-review | Luna | xhigh | Independently audit the corrected native `INPUT` ABI and generated-command contract at exact code commit `359ae89`; read-only/static validation, no Electron, shell, registry, COM, or approval-route execution. | — (no commit) | PASS: compiled helper is the only runtime size query, architecture assertions remain 40/28, generated command stays within the encoded ceiling, and focused 25/25 contracts plus TypeScript/lint evidence are consistent. | Accepted by main worker as an independent ABI review; no packaged route or product acceptance claim. |
| T03-evidence-map-37id-audit | Luna | max | Audit the 37-item T03 A02/A03 evidence map against current tests and exact journey names, without changing product behavior. | — (no commit) | Found stale `listHistory` mapping and produced the exact A/P gap matrix; the complete findings were delivered before administrative interruption. This was not a product blocker. | Accepted by main worker; findings drove the focused history-contract correction and current evidence-map reconciliation. |
| T03-list-history-contract-fix | Luna | xhigh | Close the stale `listHistory` evidence mapping with a focused repository contract test; no UI/native route or product behavior change. | `7c11d8b` | Added the revision-history facade contract and ran the focused suite 22/22, testing TypeScript, and focused ESLint successfully. | Integrated as supporting A evidence; does not close T03 or Phase 0 and requires packaged/owner evidence. |
| T03-evidence-map-count-reconciliation | Luna | xhigh | Reconcile current T03 evidence-map counts and requirement-level A/P status after the history contract correction. Docs/evidence only. | `a96e57e` | Current map records C2 4/75 and aggregate C1-C4 14/326 with stale entries removed and exact remaining gaps retained. | Integrated as current audit evidence; no P/M promotion or Phase 0/release acceptance claim. |
| T03-association-post-ABI-static-gate | Sol | high | Independently review exact HEAD `cdaf61220d89642062b6633362a56f106c5218e9` after the native `INPUT` ABI correction as a static package/list/run gate. No Electron, package build, Explorer, UIA, COM, registry, shell UI, native input, cleanup, or approval route. | `cdaf61220d89642062b6633362a56f106c5218e9` | PASS-to-package/list/run with no P0/P1 findings. The review confirmed the managed `Marshal.SizeOf(typeof(INPUT))` helper, x64/x86 40/28 gates, native Enter safety and exact activation proof, activation-neutral redacted diagnostics, restoration/finalization gates, exact selector isolation, and a realistic 29,448-character command. Windows contracts 25/25, focused lint, lifecycle 95/95, testing/desktop TypeScript, and diff checks passed. | Authorized one controlled exact association selector only with one worker and `retries=0`; no practical association success, T03, Phase 0, product, or release acceptance claim. |
| T05-T08-checkpoint-reconciliation | Luna | xhigh | Read-only audit of the accepted blank-GUI package, T05-T08 plan rows, acceptance ledger, compatibility scope, and reported startup race. No product, shell, provider, or evidence edits. | `f96cbe6` audit boundary | Confirmed package product commit `38235da`, evidence refresh `f96cbe6`, baseline ledger/J02/J03 remain nonterminal, and the package does not prove drag, quick-add, save/reopen, all-node validation, or full direct-editor scope. Reproduced the architectural race: distinct documents share `graph-root`, so graph-ID-only readiness could expose the stale canvas; no legacy catalog fallback is in product scope. | Accepted by the main worker. Findings drove document-scoped revision hydration and the focused two-document regression in `5f00933`; this is routine support, not an independent phase approval. |
| PH0-review | Sol | high | Independent Phase 0 reviewer: operate the relevant application/evidence workflow first, then inspect T01 ledger, validator, and baseline report. | pending | Pending independent review. | Must review before Phase 0 gate decision; cannot self-approve. |

Current completed-job allocation check (excluding pending `PH0-review` and this note):
48 completed jobs, Luna 17/48 = 35.4%, Terra 18/48 = 37.5%, and Sol 13/48 =
27.1%. These counts are within the required Luna 30–50%, Terra 20–40%, and
Sol 10–30% ranges. The parser counts every completed table row by its model and
excludes the pending review row; it does not infer completion from task IDs or
global test totals.

## Contract

Each implementation row records model, effort, owned scope, exact commit, findings, and disposition. Review rows are assigned before the gate and remain pending until the reviewer reports application-first findings. Status changes must be made by the integration owner after reviewing the returned diff; this file does not replace `ledger.json` evidence.
