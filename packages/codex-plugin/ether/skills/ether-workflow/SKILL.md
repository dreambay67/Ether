---
name: ether-workflow
description: Use when a previous Ether skill name needs the current Ether 4.0 workflow director.
---

# Compatibility alias

`$ether-workflow` is a compatibility alias for `$ether-director`. Use the active Ether 4.0 skill for one-document inspection, graph transactions, permits, and explicit runs.
